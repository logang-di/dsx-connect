apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dsxa-scanner.fullname" . }}-test-connection"
  labels:
    {{- include "dsxa-scanner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dsxa-scanner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
