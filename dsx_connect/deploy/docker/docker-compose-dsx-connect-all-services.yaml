x-common-env: &common-env
  DSXCONNECT_SCANNER__SCAN_BINARY_URL: "http://dsxa_scanner:5000/scan/binary/v2"
  DSXCONNECT_WORKERS__BROKER: "redis://redis:6379/5"
  DSXCONNECT_WORKERS__BACKEND: "redis://redis:6379/6"
  DSXCONNECT_REDIS_URL: "redis://redis:6379/3"
  # Results/stats DB config (Redis by default)
  DSXCONNECT_RESULTS_DB: "redis://redis:6379/3"
  DSXCONNECT_RESULTS_DB__RETAIN: 100
  PYTHONUNBUFFERED: 1

x-core-image: &core-image ${DSXCONNECT_IMAGE:-dsxconnect/dsx-connect:__VERSION__}

services:
  dsx_connect_api:
    image: *core-image
    #build:
    #  context: .
    #  dockerfile: Dockerfile
    ports:
      - "8586:8586"
    environment:
      <<: *common-env
      # TLS for API (enable and provide cert/key paths inside the container)
      # DSXCONNECT_USE_TLS: "false"
      # DSXCONNECT_TLS_CERTFILE: "/app/certs/dev.localhost.crt"
      # DSXCONNECT_TLS_KEYFILE: "/app/certs/dev.localhost.key"
      # using common-env setting for SCAN_BINARY_URL if commented out
#      DSXCONNECT_SCANNER__SCAN_BINARY_URL: "http://dsxa_scanner:5000/scan/binary/v2"
      LOG_LEVEL: debug
    depends_on:
      redis:
        condition: service_healthy  # Wait for Redis to be healthy
    networks:
      dsx-network:
        aliases:
          - dsx-connect-api
    # Updated uvicorn command with SSE-friendly settings
    command: ["python", "dsx_connect/dsx-connect-api-start.py"]
    # Health check for the API service
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8586/dsx-connect/test/connection"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # Graceful shutdown for SSE connections
    stop_grace_period: 30s
    restart: unless-stopped

  dsx_connect_scan_request_worker:
    image: *core-image
    environment:
      <<: *common-env
      # using common-env setting for SCAN_BINARY_URL if commented out
      #      DSXCONNECT_SCANNER__SCAN_BINARY_URL: "http://dsxa_scanner:5000/scan/binary/v2"
      LOG_LEVEL: debug
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - dsx-network
    command: >
      celery -A dsx_connect.taskworkers.celery_app.celery_app worker
      --loglevel=warning
      -Q dev.dsx_connect.scans.request
      --concurrency=1
    restart: unless-stopped


  dsx_connect_verdict_action_worker:
    image: *core-image
    environment:
      <<: *common-env
      LOG_LEVEL: debug
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - dsx-network
    command: >
      celery -A dsx_connect.taskworkers.celery_app.celery_app worker
      --loglevel=warning
      -Q dev.dsx_connect.scans.verdict
      --concurrency=1
    restart: unless-stopped

  dsx_connect_results_worker:
    image: *core-image
    environment:
      <<: *common-env
      LOG_LEVEL: debug
      # Point workers at the in-Compose collector via the shared alias 'syslog'.
      # If you don't run a collector (no profile enabled), unset these or point to your external collector.
      DSXCONNECT_SYSLOG__SYSLOG_SERVER_URL: syslog
      DSXCONNECT_SYSLOG__SYSLOG_SERVER_PORT: 514
      DSXCONNECT_SYSLOG__TRANSPORT: tcp
      DSXCONNECT_FRONTEND__URL: "http://dsx-connect-api:8586"
    depends_on:
      redis:
        condition: service_healthy
      # No hard dependency on a collector: start even if no profile is enabled
    networks:
      - dsx-network
    command: >
      celery -A dsx_connect.taskworkers.celery_app.celery_app worker
      --loglevel=warning
      -Q dev.dsx_connect.scans.result
      --concurrency=1
    restart: unless-stopped

  dsx_connect_notification_worker:
    image: *core-image
    environment:
      <<: *common-env
      LOG_LEVEL: debug
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - dsx-network
    command: >
      celery -A dsx_connect.taskworkers.celery_app.celery_app worker
      --loglevel=warning
      -Q dev.dsx_connect.scans.result.notify
      --concurrency=1
    restart: unless-stopped

  redis:
    image: redis:7-alpine  # Use newer version
    networks:
      - dsx-network
    # Redis configuration optimized for pubsub and SSE
    command: >
      redis-server 
      --notify-keyspace-events Ex 
      --maxmemory-policy allkeys-lru 
      --timeout 0 
      --tcp-keepalive 300
      --appendonly no
    # Health check for Redis
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    restart: unless-stopped
    ## Uncomment for persistence if needed
    # volumes:
    #  - redis-data:/data
    # command: [ "redis-server", "--appendonly", "yes" ]

#  # Optional: Add nginx reverse proxy for SSE (recommended for production)
#  nginx:
#    image: nginx:alpine
#    ports:
#      - "80:80"
#    volumes:
#      - ./nginx-sse.conf:/etc/nginx/nginx.conf:ro
#    depends_on:
#      dsx_connect_api:
#        condition: service_healthy
#    networks:
#      - dsx-network
#    restart: unless-stopped
#    # Only include if you create the nginx-sse.conf file

  # Syslog collectors (choose exactly one profile: 'syslog-ng' or 'rsyslog').
  # Both expose a network alias 'syslog' so workers can always use host 'syslog'.
#  syslog_ng:
#    image: balabit/syslog-ng:latest
#    profiles: ["syslog-ng"]
#    networks:
#      dsx-network:
#        aliases: ["syslog", "syslog-ng"]
#    restart: unless-stopped
#    user: "0:0"
#    cap_add: ["NET_BIND_SERVICE"]
#    entrypoint: ["/bin/sh","-lc"]
#    command:
#      - |
#        cat > /etc/syslog-ng/syslog-ng.conf << 'EOF'
#        @version: 3.38
#        options { chain-hostnames(no); flush-lines(1); keep-hostname(yes); create-dirs(yes); };
#        source s_net {
#          syslog(ip("0.0.0.0") transport("tcp") port(514));
#          syslog(ip("0.0.0.0") transport("udp") port(514));
#        };
#        destination d_stdout { file("/dev/stdout"); };
#        log { source(s_net); destination(d_stdout); };
#        EOF
#        exec syslog-ng -F -f /etc/syslog-ng/syslog-ng.conf

  rsyslog:
    image: rsyslog/rsyslog
    profiles: ["rsyslog"]
    networks:
      dsx-network:
        aliases: ["syslog", "rsyslog"]
    restart: unless-stopped
    user: "0:0"
    cap_add: ["NET_BIND_SERVICE"]
    entrypoint: ["/bin/sh","-lc"]
    command:
      - |
        cat > /etc/rsyslog.conf << 'EOF'
        module(load="imuxsock")
        module(load="imtcp")
        module(load="imudp")
        input(type="imtcp" port="514")
        input(type="imudp" port="514")
        template(name="rawMsg" type="string" string="%msg%\n")
        # Write all messages to container stdout for docker compose logs
        *.* action(type="omfile" file="/dev/stdout" template="rawMsg")
        EOF
        exec rsyslogd -n -f /etc/rsyslog.conf

networks:
  dsx-network:
    external: true
    name: dsx-connect-network

# Uncomment if using Redis persistence
# volumes:
#   redis-data:
