apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-s3-connector.fullname" . }}
  labels:
    {{- include "aws-s3-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.tls.enabled }}
    - port: 443
      targetPort: https
      protocol: TCP
      name: https
    {{- end }}
  selector:
    {{- include "aws-s3-connector.selectorLabels" . | nindent 4 }}
