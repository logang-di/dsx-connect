# Env for Azure Blob connector. Pin the image and set storage credentials + asset. Copy to .azure-blob.env and use with --env-file.

# Image tag to pin (override default :latest)
AZURE_BLOB_IMAGE=dsxconnect/azure-blob-storage-connector:0.5.36

# Target container/path
DSXCONNECTOR_ASSET=your-container
DSXCONNECTOR_FILTER=

# Auth: use ONE of the following (preferred: connection string)
# 1) Connection string (recommended)
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;AccountKey=your-key;EndpointSuffix=core.windows.net
# 2) Or account/key (leave connection string unset)
# AZURE_STORAGE_ACCOUNT=your-account
# AZURE_STORAGE_KEY=your-key

# Enrollment (optional)
# DSXCONNECT_ENROLLMENT_TOKEN=abc123
