# Env for GCS connector. Pin the image and set GCP creds + bucket/filter. Copy to .env.gcs.<env> and use with --env-file.

# Google Cloud Storage connector env (sample)
GCS_IMAGE=dsxconnect/google-cloud-storage-connector:0.5.30
GOOGLE_APPLICATION_CREDENTIALS=/app/creds/gcp-sa.json
DSXCONNECTOR_ASSET=your-bucket
DSXCONNECTOR_FILTER=
#DSXCONNECT_ENROLLMENT_TOKEN=abc123