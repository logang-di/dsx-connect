# Env for SharePoint connector. Pin the image and set tenant/app creds + asset/filter. Copy to .env.sharepoint.<env> and use with --env-file.

# SharePoint connector env (sample)
SHAREPOINT_IMAGE=dsxconnect/sharepoint-connector:0.3.31
SP_TENANT_ID=
SP_CLIENT_ID=
SP_CLIENT_SECRET=
DSXCONNECTOR_ASSET=your-site/your-library
DSXCONNECTOR_FILTER=
#DSXCONNECT_ENROLLMENT_TOKEN=abc123
